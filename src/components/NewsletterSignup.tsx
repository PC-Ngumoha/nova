// import classNames from '@/components/styles/NewsletterSignup.module.scss';

const NewsletterSignup = () => {
  return <span>Newsletter signup form will show up here</span>;
};

export default NewsletterSignup;
